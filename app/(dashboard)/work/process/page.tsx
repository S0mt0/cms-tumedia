import { CmsEditorPageShell } from "@/components/common/cms-editor-page-shell"; import { workRepository } from "@/lib/db/repositories/work.repository"; import { WorkFields } from "../_components/work-fields"; export default async function Page(){const {process}=await workRepository.get();return <CmsEditorPageShell eyebrow="Work" title="Campaign process" description="Edit the campaign-flow timeline." breadcrumbs={[{label:"Overview",href:"/"},{label:"Work",href:"/work"},{label:"Process"}]}><WorkFields section="process" initial={process} title="Campaign process" description="The stages displayed on the public timeline."/></CmsEditorPageShell>}
