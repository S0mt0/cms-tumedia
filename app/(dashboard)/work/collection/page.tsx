import { CmsEditorPageShell } from "@/components/common/cms-editor-page-shell"; import { workRepository } from "@/lib/db/repositories/work.repository"; import { WorkFields } from "../_components/work-fields"; export default async function Page(){const {collection}=await workRepository.get();return <CmsEditorPageShell eyebrow="Work" title="Campaign reels" description="Manage campaign categories and YouTube reels." breadcrumbs={[{label:"Overview",href:"/"},{label:"Work",href:"/work"},{label:"Campaign reels"}]}><WorkFields section="collection" initial={collection} title="Campaign reels" description="Each item appears in the Work collection."/></CmsEditorPageShell>}
