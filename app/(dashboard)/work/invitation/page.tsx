import { CmsEditorPageShell } from "@/components/common/cms-editor-page-shell"; import { workRepository } from "@/lib/db/repositories/work.repository"; import { WorkFields } from "../_components/work-fields"; export default async function Page(){const {invitation}=await workRepository.get();return <CmsEditorPageShell eyebrow="Work" title="Invitation" description="The final Work-page call to action." breadcrumbs={[{label:"Overview",href:"/"},{label:"Work",href:"/work"},{label:"Invitation"}]}><WorkFields section="invitation" initial={invitation} title="Final invitation" description="The final campaign invitation and CTA."/></CmsEditorPageShell>}
